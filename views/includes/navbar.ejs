<nav class="navbar navbar-expand-md bg-light border-bottom shadow-sm sticky-top">
  <div class="container-fluid px-3">

    <!-- Logo -->
    <a class="navbar-brand d-flex align-items-center me-2" href="/">
      <img src="/images/tripinLogo.png" alt="Tripin Logo" class="img-fluid" style="height: 48px;" />
    </a>

    <!-- Hamburger Toggler (Mobile) -->
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible Content -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">

      <!-- Left Links -->
      <div class="navbar-nav ms-2">
        <a class="nav-link text-dark fw-semibold" href="/">Home</a>
        <a class="nav-link text-dark fw-semibold" href="/listings">All Listings</a>
        <a class="nav-link text-dark fw-semibold" href="/listings/new">Add Listing</a>
      </div>


      <!-- Right Auth Section -->
<div class="navbar-nav ms-auto mt-3 mt-md-0 d-flex align-items-center">
  <% if (!currUser) { %>
    <a class="btn btn-outline-dark me-2 mb-2 mb-md-0" href="/signup">
      <i class="fa-solid fa-user-plus me-1"></i> Sign Up
    </a>
    <a class="btn btn-dark mb-2 mb-md-0" href="/login">
      <i class="fa-solid fa-right-to-bracket me-1"></i> Login
    </a>
  <% } else { %>
    <div class="d-flex align-items-center bg-light px-3 py-1 rounded shadow-sm me-3">
      <i class="fa-solid fa-user text-primary me-2"></i>
      <span class="fw-semibold text-dark">Hi, <%= currUser.username %></span>
    </div>
    <a class="btn btn-danger mb-2 mb-md-0" href="/logout">
      <i class="fa-solid fa-arrow-right-from-bracket me-1"></i> Logout
    </a>
  <% } %>
</div>
    </div>
  </div>
</nav>